<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".screen_order.ContentScreenOrder"
    android:background="@drawable/border_radius_15dp"
    android:backgroundTint="@color/off_white">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="30dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/off_white">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycle_item_content_screen_order"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scrollbars="none"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:listitem="@layout/item_content_screen_order" />

            <TextView
                android:id="@+id/title_input_screen_order"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/title_input_observations"
                android:textSize="24sp"
                android:fontFamily="@font/anton"
                android:textColor="@color/yellow"/>

            <EditText
                android:id="@+id/input_observations_screen_order"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="20dp"
                android:padding="10dp"
                android:background="@drawable/input_screen_order"
                android:hint="@string/text_placeholder_observations"
                android:textColorHint="@color/grey_"
                android:fontFamily="@font/anton"
                android:textSize="16sp"
                android:textColor="@color/black"
                android:gravity="top"/>

        </LinearLayout>

    </ScrollView>

</FrameLayout>